<div>
  <p>trophies go here</p>
</div>

<div>
  <h2> Based on Preference </h2>
  <% if @based_on_preference == nil %>
    <p>You have cleared this category, please try another?</p>
  <% else %>
    <% @based_on_preference.each do |beer| %>
    <p><%= beer.name %></p>
    <img src="<%= beer.image_thumb_url %>"></br>
    <% end %>
  <% end %>
</div>

<div>
  <h2>Based On Previous</h2>
  <% @based_on_previous.each do |beer| %>
    <p><%= beer.name %></p>
    <img src="<%= beer.image_thumb_url %>">
  <% end %>
  <p>suggestions based on previous selections</p>
</div>

<div class="">
  <h2>Most Recent Beers</h2>
  <% @user_recent_4.each do |ub| %>
    <p><%= ub.beer.name %></p>
    <p><%= ub.beer.category.category_type %></p>
    <p><%= ub.beer.varietal %></p>
    <img src="<%= ub.beer.image_thumb_url %>"></br>
  <% end %>
</div>
