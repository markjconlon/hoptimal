<div class="edituser">
  <%= link_to "EDIT MY PROFILE", edit_user_path("current"), :class => "btn btn-default btn-primary" %>
</div>

<div class="random">
<%= render 'random' %>
</div>

<div class="preferences">
<%= render 'preferences' %>
</div>

<div class="suggestions">
<%= render 'suggestions' %>
</div>

<div class="recent_beer">
<%= render 'recent' %>
</div>


<a href="#" class="back-to-top btn btn-default btn-primary">Back to Top</a>
