<%= search_form_for @q do |f| %>

  <%= f.label :beer_name_cont %>
  <%= f.search_field :beer_name_cont, id:"search-field" %>

  <%= f.submit %>
<% end %>

<div class="recent_beer">

<h2>
  My Beers
</h2>

<% @user_beers.each do |ub| %>
  <div class="beer_consumed">
    <a href="#" id="beer_image">
      <div class="beer_img">
        <img src="<%= ub.beer.image_thumb_url %>"></br>
      </div>
    </a>

      <div class ="beer_info">
        <p><%= ub.beer.name %></p>
        <p><%= ub.beer.category.category_type %></p>
        <p>Rating: <%= ub.rating %></p>
        <a href="/user_beers/<%=ub.id%>/edit">EDIT MY BEER</a>
        <p><%= link_to 'REMOVE FROM MY LIST',  { action: :destroy, id: ub.id }, method: :delete, data: { confirm: 'Are you sure?' } %></p>
      </div>
</div>
  <% end %>
