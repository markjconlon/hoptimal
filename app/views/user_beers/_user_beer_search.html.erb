<% @user_beers.each do |ub| %>
  <div class="beer_consumed">
      <div class="beer_img">
        <img src="<%= ub.image_thumb_url %>"></br>
      </div>


      <div class ="beer_info">
        <p><%= ub.name %></p>
        <p><%= ub.category.category_type %></p>
        <% ub_beer_id = ub.id  %>
        <p>Rating: <%= UserBeer.find_by(beer_id: ub_beer_id).rating %></p>
        <a href="/user_beers/<%=ub.id%>/edit">EDIT MY BEER</a>
        <p><%= link_to 'REMOVE FROM MY LIST',  { action: :destroy, id: ub.id }, method: :delete, data: { confirm: 'Are you sure?' } %></p>
      </div>
  </div>
<% end %>
